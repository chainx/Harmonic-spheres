"""Construct the Jarvis-Norbury loop map with a minimal direct disk solver.

    1. pull the connection back to one disk D_w,
    2. solve (d/dzbar + A_zbar) g = 0 on a polar grid,
    3. enforce boundary unitarity and a based normalization,
    4. read off the boundary loop to get a map w ↦ ΩG
"""

def main():
    pass

if __name__ == "__main__":
    main()
